# Project Seed — ai-research

> Auto-generated by `/project-init` on 2026-04-14. Consumed by `/brainstorm`.

## Objective

Implement the ideas in Karpathy's "LLM Wiki" gist (https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f): an LLM-maintained, persistent markdown wiki that replaces ad-hoc RAG. The LLM incrementally ingests raw sources and produces/updates interconnected pages (summaries, cross-references, contradiction flags). Three layers: raw sources (immutable) → wiki (LLM-generated pages) → schema (config for structure and workflows). Humans curate sources and ask questions; the LLM handles maintenance.

## Tech Stack

- **Language**: Python 3.12+
- **Package manager**: uv
- **Framework**: FastAPI (optional API surface)
- **Runtime**: Local CLI

## Architecture

CLI tool. Reads from a `sources/` directory, writes to a `wiki/` directory of interconnected markdown pages, driven by a schema config file. LLM provider abstraction so it can run against hosted APIs (Anthropic/OpenAI) and, ideally, local models (Ollama) later.

## Repo

- **Name**: ai-research
- **Visibility**: Private
- **GitHub URL**: https://github.com/fxmartin/ai-research
- **Created**: 2026-04-14

## Notes

- **Obsidian-compatible output**: wiki pages must use Obsidian-friendly markdown — `[[wikilinks]]` for cross-references and YAML frontmatter for metadata. The wiki directory should be openable as an Obsidian vault with no conversion step.
- **Inspiration**: Vannevar Bush's Memex (1945); community extensions on the gist mention formal ontologies, token optimization, graph databases, research pipelines — candidates for later epics.
- **Open design questions for `/brainstorm`**: schema format (YAML/TOML/Python), LLM provider abstraction, incremental update strategy (diffing sources, invalidating pages), contradiction detection, local-LLM support, optional web UI vs pure CLI.
